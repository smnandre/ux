<div {{ attributes }}>
    <p data-current-page>{{ this.pagination.currentPage }}</p>

    {{ ux_pagination(
        this.pagination,
        linkAttributes: this.paginationLinkAttributes,
    ) }}
</div>
